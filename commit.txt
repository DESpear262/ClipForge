feat(export, ui): complete PR #8; replace menu Export with Delete; robust deletion

- Backend (Rust/Tauri):
  - Added `export_video` command calling new `export_trim` (FFmpeg with `-progress pipe:1`)
  - Emits `export:start|progress|success` events; fixed `emit_all` by importing `tauri::Manager`
  - Implemented `open_export_dialog` save dialog
- Frontend:
  - Added `useExport` hook to invoke export and listen to progress
  - MediaLibrary: Export Trim button with percent status and error display
  - MenuBar: Replaced Export with Delete; dispatches `request-delete`
  - MediaLibrary: Handles `request-delete` with confirmation, DB deletion, reload, next selection
  - Persist last selected clip via ref so delete works even if UI transiently deselects
- Timeline enhancements (follow-up): dynamic min zoom fits full duration; snap playhead to in after gating
- Logging: Added targeted logs for delete flow, reduced noisy selection logs

docs(memory-bank):
- Marked PR #8 complete; updated activeContext/progress with export and delete behaviors
- Documented export pipeline (args, events) and menu delete pattern; noted last-selected fallback
