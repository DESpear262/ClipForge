docs(core): align project to Tauri v1; implement Konva timeline (PR #6) with bi-directional seek

- Standardized docs to Tauri v1:
  - Updated Memory Bank (`projectbrief.md`, `systemPatterns.md`, `techContext.md`) and README
  - Removed v2 capability references; documented v1 allowlist usage
- Implemented PR #6 (Konva Timeline):
  - Added `TimelineContext` with currentTime/duration/pxPerSecond and seek registration
  - Added `Timeline.tsx` (grid, single-clip bar, playhead, zoom slider, interaction overlay)
  - Wired timeline under `MediaLibrary` with `TimelineProvider`
  - Added bi-directional sync with `VideoPlayer` (`onTimeUpdate` -> timeline; `onReady.seek` -> player)
- Fixed issues:
  - Resolved `formatTime` initialization error in timeline
  - Fixed `onReady` props destructuring bug causing seek registration failure
  - Improved interaction reliability with transparent overlay; added concise seek logs

Result: Timeline displays and tracks time during playback; clicking/dragging on timeline seeks video reliably. Ready for PR #7 (trim handles) and PR #8 (export pipeline).

