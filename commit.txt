feat(timeline): complete PR #7 trim UI; add fit-to-duration zoom and playhead snap

- Implemented PR #7 (Trim Handles):
  - Left/right handles with snapping (whole seconds, playhead; Alt disables)
  - Dragging inside selection moves window; keyboard precision (0.05s, Shift=0.5s)
  - Loop trim toggle; playback pauses at out when loop is off
  - Persist per-clip trim in project state; mirrored via TimelineContext
- UX improvements:
  - Dynamic minimum zoom so full video fits at max zoom-out
  - After setting gates, playhead snaps to in-point if outside
- Stability fixes:
  - Moved selection init into useEffect; fixed provider ordering errors
  - Removed noisy MediaLibrary selection logs

Docs (memory-bank):
- Updated activeContext and progress to reflect PR #7 completion and new timeline rules
- Added systemPatterns timeline interaction rules

