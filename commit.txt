feat: implement metadata extraction with FFprobe integration

PR #4 - Metadata Extraction (FFprobe)

Complete video metadata extraction and display:
- Enhanced VideoMetadata type with containerFormat and fps fields
- Updated Rust ffmpeg.rs to extract additional metadata (format_name, r_frame_rate)
- Added duration rounding to nearest 0.1s and fps parsing from fraction format
- Created ClipCard component to display metadata in a compact card format
- Added metadata formatting utilities (duration, file size, bitrate, resolution, fps)
- Updated useImport to auto-probe metadata immediately after import
- Modified ImportZone to show ClipCard instead of basic metadata display
- Added retry functionality for failed metadata probes
- Implemented user-friendly error handling with toast notifications
- Fixed TypeScript verbatimModuleSyntax import errors (type-only imports)
- Fixed button text colors for better readability (black text on buttons)

Files created:
- src/components/ClipCard.tsx - Metadata display component
- src/utils/formatters.ts - Formatting utilities for display

Files modified:
- src/types/media.ts - Enhanced VideoMetadata interface
- src-tauri/src/ffmpeg.rs - Enhanced probe_metadata with additional fields, fixed ownership error
- src/hooks/useImport.ts - Auto-probe after import
- src/components/ImportZone.tsx - Use ClipCard for metadata display
- src/components/ErrorBoundary.tsx - Fixed type-only imports
- src/hooks/useToast.ts - Removed unused import

Metadata extracted: duration, resolution, codec, file size, bitrate, container format, fps
