feat(app): finalize MVP polish, packaging/startup, and persistence

- UI/UX
  - Centered header title; full-window layout fix (no white gutter)
  - Tidy timeline footer (In/Out/Len box); metadata card styling
  - Moved Export to top bar; improved spacing in right panel
- Timeline/Trim
  - Handles with snapping (secs/playhead, Alt to disable); selection move; keyboard precision
  - Dynamic min zoom to fit full duration; playhead snaps to in if outside gates
- Export & Preview
  - Export pipeline with progress events; percent toast and success toast
  - Preview generation start/progress/success toasts; import success toast
- Delete flow
  - Menu Delete dispatch with robust handler; last-selected fallback to avoid deselection race
- Persistence
  - JSON project state in AppData: trims by path, timeline zoom, loop toggle, last-selected
- Packaging/Startup (Tauri v1)
  - Correct custom protocol feature mapping:
    - Cargo features: default=[custom-protocol], custom-protocol=[tauri/custom-protocol]
  - Explicit main window creation in code:
    - Dev → External("http://localhost:1420")
    - Prod → App("index.html")
  - Vite base='./' to ensure relative asset paths; embedded assets load in release
  - Sidecars (ffmpeg/ffprobe) bundled; note: for raw exe testing, copy bin/ next to exe

Result: Packaged app boots to embedded UI (no localhost dev URL); MVP loop Import → Preview → Trim → Export is complete with toasts and persistence.

